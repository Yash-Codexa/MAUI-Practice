<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:DataTemplates="clr-namespace:PagesDemo.Selectors"
             x:Class="PagesDemo.Views.CollectionView_Demo_View"
             Title="CollectionView_Demo_View">

    <ContentPage.Resources>
        <DataTemplates:ProductDataTemplate_Selector x:Key="ProductTemplates" />
    </ContentPage.Resources>

    <RefreshView Command="{Binding RefreshCommand}" IsRefreshing="{Binding IsRefreshing}"> 

        <CollectionView ItemsSource="{Binding Products}" 
            ItemTemplate="{StaticResource ProductTemplates}" 
            RemainingItemsThreshold="1"
            RemainingItemsThresholdReachedCommand="{Binding ThresholdReachedCommand}"
        >
    </CollectionView>

    </RefreshView>
    
</ContentPage>